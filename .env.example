# Copy this file to .env at the repo root for docker-compose, and copy the
# relevant values into apps/api/.env and apps/web/.env.local as well.

# ---- Postgres (docker-compose) ----
POSTGRES_USER=dautaytoy
POSTGRES_PASSWORD=dautaytoy_dev_password
POSTGRES_DB=dautaytoy

NODE_ENV=development
